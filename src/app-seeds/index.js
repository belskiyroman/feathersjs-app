const currencies = require('./curencies.seed');
const levels = require('./levels.seed');
const roles = require('./roles.seed');
const statuses = require('./statuses.seed');

module.exports = {
  currencies,
  levels,
  roles,
  statuses,
};
